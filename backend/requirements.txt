# ============================================================
# $GROWTH — Autonomous Marketing Growth Hacker (Backend)
# Python 3.11 | FastAPI + Asyncio + Solana + OpenAI
#
# NOTE: Production deployments should replace loose pins with
# hashed, locked artifacts (see requirements.lock.txt / pip-tools).
# Versions below are the latest known-stable as of the 2026 line.
# ============================================================

# ---- Web framework & server ---------------------------------
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20            # form / file uploads (if needed)

# ---- Async HTTP client (shared by solana-py & OpenAI) --------
httpx==0.28.1
aiohttp==3.11.18                    # optional alt client / websockets (3.11.14 yanked)

# ---- Configuration & env management --------------------------
pydantic==2.11.3                    # settings validation (BaseSettings)
pydantic-settings==2.9.1            # env -> typed Settings models
python-dotenv==1.1.0                # .env loading
PyYAML==6.0.2                       # optional YAML config surface

# ---- Solana (Python) -----------------------------------------
# NOTE: solana-py 0.36.x pins solders to >=0.23,<0.28 — pin solders within
# that range so the resolver never picks an incompatible combination.
solana==0.36.11                     # solana-py RPC client
solders==0.27.1                     # Rust-backed primitives (keys, tx)
base58==2.1.1                       # base58 encode/decode for keys

# ---- AI Engine (OpenAI) --------------------------------------
openai==1.72.0                      # AsyncOpenAI (gpt-4o-mini)

# ---- Observability / scheduling -------------------------------
structlog==25.2.0                   # structured, async-friendly logging
apscheduler==3.11.0                 # optional cron-style triggers
tenacity==9.1.2                     # retries w/ backoff for RPC / LLM

# ---- Dev / test tooling ---------------------------------------
pytest==8.3.5
pytest-asyncio==0.26.0
ruff==0.11.4                        # lint + format
